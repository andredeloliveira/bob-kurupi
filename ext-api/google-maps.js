/*loads the Google Maps API on the startup of the application --- to save time during several requests*/

// if(Meteor.isClient){
//   Meteor.startup(function(){
//       GoogleMaps.load({v: '3', key: '', libraries:'geometry, places'});
//       //ok! after this the API should be ready for user
//   });
//
// }
